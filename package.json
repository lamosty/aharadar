{
  "name": "aharadar",
  "private": true,
  "version": "0.0.0",
  "description": "Personalized, topic-agnostic content aggregation + ranking system (spec-first).",
  "license": "MIT",
  "packageManager": "pnpm@9.15.0",
  "scripts": {
    "build": "pnpm -r build",
    "dev": "pnpm -r dev",
    "test": "pnpm -r test",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  }
}


