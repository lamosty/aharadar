# ---- Core ----
APP_ENV=local
APP_TIMEZONE=UTC

# ---- Database ----
POSTGRES_HOST=localhost
POSTGRES_PORT=5432
POSTGRES_DB=aharadar
POSTGRES_USER=aharadar
POSTGRES_PASSWORD=aharadar_dev_password

# Full connection string (preferred by apps)
DATABASE_URL=postgres://aharadar:aharadar_dev_password@localhost:5432/aharadar

# ---- Redis (BullMQ) ----
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_URL=redis://localhost:6379

# ---- API (optional MVP) ----
API_HOST=0.0.0.0
API_PORT=8080
ADMIN_API_KEY=dev_admin_key_change_me

# ---- LLM (OpenAI Responses API; router stays for future) ----
# Required when triage/enrichment is enabled:
# OPENAI_API_KEY=
# OPENAI_ENDPOINT=   # Full /v1/responses URL (preferred) OR
# OPENAI_BASE_URL=   # Base URL; /v1/responses is appended
#
# Optional model routing:
# OPENAI_MODEL=
# OPENAI_TRIAGE_MODEL=
# OPENAI_TRIAGE_MODEL_LOW=
# OPENAI_TRIAGE_MODEL_NORMAL=
# OPENAI_TRIAGE_MODEL_HIGH=
#
# Optional triage limits:
# OPENAI_TRIAGE_MAX_OUTPUT_TOKENS=250  # raise (e.g. 900+) if OPENAI_TRIAGE_REASONING_EFFORT is set
# OPENAI_TRIAGE_MAX_INPUT_CHARS=4000
# OPENAI_TRIAGE_MAX_TITLE_CHARS=240
# OPENAI_TRIAGE_MAX_CALLS_PER_RUN=
#
# Optional triage reasoning (GPT-5.1+):
# OPENAI_TRIAGE_REASONING_EFFORT=high
#
# Optional credits estimate:
# OPENAI_CREDITS_PER_1K_INPUT_TOKENS=
# OPENAI_CREDITS_PER_1K_OUTPUT_TOKENS=
# OPENAI_TRIAGE_CREDITS_PER_1K_INPUT_TOKENS=
# OPENAI_TRIAGE_CREDITS_PER_1K_OUTPUT_TOKENS=

# ---- Signal Providers (X/Twitter search; Grok example) ----
# GROK_API_KEY=
# GROK_BASE_URL=

# ---- Budget defaults (credits; example) ----
MONTHLY_CREDITS=60000
DAILY_THROTTLE_CREDITS=2500
DEFAULT_TIER=normal
