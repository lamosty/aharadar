import type { FetchParams, FetchResult } from "@aharadar/shared";

export async function fetchYoutube(_params: FetchParams): Promise<FetchResult> {
  // TODO: implement YouTube fetch (channel RSS; optional transcript).
  return { rawItems: [], nextCursor: {} };
}
