import type { FetchParams, FetchResult } from "@aharadar/shared";

export async function fetchHn(_params: FetchParams): Promise<FetchResult> {
  // TODO: implement HN fetch (Firebase API).
  return { rawItems: [], nextCursor: {} };
}
